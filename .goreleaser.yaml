# yaml-language-server: $schema=https://goreleaser.com/static/schema.json

version: 2

project_name: agent-api-gateway

snapshot:
  version_template: 0.0.0-snapshot.{{ .ShortCommit }}

builds:
  - id: agent-api-gateway
    main: ./cmd/agent-api-gateway
    binary: agent-api-gateway
    env: [CGO_ENABLED=0]
    goos: [darwin, linux, windows]
    goarch: [amd64, arm64]
    ldflags:
      - >-
        -s -w
        -X github.com/sargunv/agent-api-gateway/internal/cli.Version={{ .Version }}
        -X github.com/sargunv/agent-api-gateway/internal/cli.Commit={{ .Commit }}

archives:
  - id: agent-api-gateway
    ids: [agent-api-gateway]
    formats: [tar.gz]
    name_template: >-
      agent-api-gateway_
      {{- .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
    format_overrides:
      - goos: windows
        formats: [zip]

dockers_v2:
  - ids: [agent-api-gateway]
    images: [
      '{{ with index .Env "IMAGE" }}{{ . }}{{ else }}agent-api-gateway{{ end }}',
    ]
    tags: ["{{ .Version }}", latest]
    dockerfile: Dockerfile
    labels:
      org.opencontainers.image.source: "{{ .GitURL }}"
      org.opencontainers.image.revision: "{{ .FullCommit }}"
      org.opencontainers.image.version: "{{ .Version }}"

checksum:
  name_template: checksums.txt

changelog:
  use: github
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
