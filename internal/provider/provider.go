// Package provider builds provider accounts and exposes registry helpers.
package provider

import "github.com/sargunv/agent-api-gateway/internal/model"

type (
	Account  = model.Account
	Endpoint = model.Endpoint
	Route    = model.Route
	Model    = model.Model
)
